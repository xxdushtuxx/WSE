<h1>Home</h1>
<% if notice.present? %>
    <div class="notice"><%= notice %></div>
  <% end %>
<% if logged_in? %>
      <%= link_to 'Log Out', logout_path, method: :delete %>
    <% else %>
      <%= link_to 'Login', login_path %>
    <% end %>